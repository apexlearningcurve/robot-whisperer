[tool.poetry]
name = "robot-whisperer"
version = "0.1.0"
description = ""
authors = ["--global <studen.m98@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
datasets = "^2.20.0"
jupyter = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
